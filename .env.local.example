# Copy this to .env.local and fill in real values. See SETUP.md for where to get each.

# Supabase (https://supabase.com/dashboard/project/_/settings/api)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Google AI Studio — Gemini 2.5 Flash + text-embedding-004 (https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=YOUR_GEMINI_KEY

# OpenAI — GPT-4.1 mini fallback (https://platform.openai.com/api-keys)
OPENAI_API_KEY=YOUR_OPENAI_KEY

# Groq — weekly personalized tips, Llama 3.3 70B, free (https://console.groq.com/keys)
# Optional: leave blank to disable weekly tips. No tips will be generated without it.
GROQ_API_KEY=YOUR_GROQ_KEY

# Auth — random 32+ char string. Generate with: openssl rand -base64 48
JWT_SECRET=replace_with_long_random_string

# Cron auth — random string. Vercel sends this in the Authorization header on cron invocations.
CRON_SECRET=replace_with_another_long_random_string
